# Runtime API — dépendances directes uniquement (pip résout le reste).
# Recréer le venv : python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Voir aussi requirements-dev.txt pour black, pytest, etc.

fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35
python-dotenv>=1.0,<2
motor>=3.6,<4
pymongo>=4.6,<5
pydantic[email]>=2.9,<3
bcrypt>=4.1,<5
PyJWT>=2.9,<3
aiofiles>=24.1,<26
aiohttp>=3.10,<4
stripe>=11,<16
python-multipart>=0.0.9,<0.1
